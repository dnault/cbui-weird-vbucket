This program writes a document to Couchbase in a non-standard vbucket.

To run:

    ./gradlew run --args='--cluster=localhost --bucket=default \
        --username=Administrator --password=password'
